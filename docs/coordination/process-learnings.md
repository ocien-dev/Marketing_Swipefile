# Aprendizados do processo

Registro canônico de fricções recorrentes e prevenções do Marketing Swipe File.
O chat ativo atualiza este arquivo no encerramento de um épico. O objetivo é
transformar repetição em regra executável, não acumular relatos de sessão.

## Critério de entrada

- Primeira ocorrência: registrar apenas no execution log ou no fechamento local
  do épico.
- Segunda ocorrência concreta: criar ou atualizar uma entrada aqui.
- Terceira ocorrência: impedir a quarta repetição do mesmo caminho até haver
  prevenção ou alternativa materialmente diferente.
- Risco crítico, corrupção possível ou quebra de proteção: promover de imediato.

Uma entrada só conta como recorrência quando há evidência da mesma causa, e não
apenas sintomas parecidos. Aprendizados já prevenidos por código ou teste ficam
como `promoted`; limitações externas aceitas ficam como `accepted_limitation`.

## Destino da promoção

| Natureza | Destino preferencial |
| --- | --- |
| Falha determinística recorrente | script, guard ou teste |
| Regra estável de execução | `AGENTS.md` |
| Heurística específica da extração gold | skill ou prompt gold |
| Decisão de projeto | plano e `docs/execution-log.md` |
| Limitação externa não corrigível | esta lista como limitação conhecida |

Não duplicar a mesma regra em todos os destinos. A documentação aponta para o
mecanismo canônico; testes e scripts são preferidos quando puderem impedir a
falha antes da escrita.

## Registro

| ID | Estado | Escopo | Evidência e causa | Prevenção permanente | Promovido para |
| --- | --- | --- | --- | --- | --- |
| MSF-PL-001 | promoted | patches de gold e ledger | Em v6lu, tentou-se editar manualmente decisões de ledger ausentes; o ledger canônico é derivado da evidência dos candidatos durante o build. | Alterar a evidência source-backed, rederivar e validar o ledger; só criar `ledger_decisions` quando o contrato realmente exigir decisão manual. | skill gold e fluxo transacional existente |
| MSF-PL-002 | promoted | hashes de packet | CRLF/LF produziu hashes físicos diferentes para JSON semanticamente idêntico. | Comparar hash físico e hash semântico JSON e tratar newline isolado como provenance de serialização. | Fast Path 002 e skill gold |
| MSF-PL-003 | promoted | carga ativa de waves | Planejar somente por quantidade de episódios ocultava waves com muitos chunks ativos. | Orçar segmentos/chunks ativos e dividir revisão em faixas planejadas de 8–12 chunks. | `run_gold_wave.py` e skill gold |
| MSF-PL-004 | promoted | remediação gold | Helpers ad hoc e reaplicações geraram loops e risco de estado parcial. | Exigir `--check` read-only, asserts/precondições, um único `--apply` atômico e validação final. | `gold_review_patch.py`, testes e skill gold |
| MSF-PL-005 | retired | transporte entre chats | O transporte de eventos entre papéis separados acrescentou latência, custo e falhas de continuidade. | Modelo removido pelo owner; executar o épico integralmente no chat ativo e reservar troca de modelo apenas para a auditoria final. | `AGENTS.md` e contrato gold |
| MSF-PL-006 | promoted | autocheck pré-build | Em Wave 005, 3.009 sinais viraram pendência porque a prévia consultava somente ledger manual antes do ledger derivado existir. | Usar `ledger_for_signals()` em memória antes do build e manter pendente apenas decisão inválida ou ausente. | Fast Path 003, regressões e diagnóstico read-only; validado por 0 previews na Wave 005 |
| MSF-PL-007 | promoted | unidade de execução da wave | Mesmo após a simplificação, erros mecânicos de payload e checkpoints de chunks encerraram turnos e provocaram retornos sucessivos ao coordenador. A causa era a validação fail-fast acoplada ao recorder e a ausência de um gate consolidado de cinco episódios. | Compilador read-only com inventário completo, recorder idempotente verificável e gate 5/5 que não emite receipt em progresso e vincula cada packet ao episódio/manifesto. | `MSF-R20-GOLD-WAVE-ONE-SHOT-001`, aprovado com 74 testes e probes adversariais |
| MSF-PL-008 | retired | continuidade automatizada | Self-message, heartbeat e runner persistente não entregaram avanço proporcional e aumentaram o custo operacional. | Não automatizar continuidade do chat; executar diretamente e tratar apenas ações individuais sem progresso por 30 minutos. | `AGENTS.md`; automação e runner removidos |
| MSF-PL-009 | promoted | encerramento direto de waves | A Wave 005 só avançou de forma consistente quando a execução, as correções source-backed, o gate 5/5 e a revisão final ficaram no mesmo chat. Coordenação inter-chat multiplicou contexto, transporte e paradas sem aumentar a qualidade. | Executar todo o épico no chat ativo; reservar `gpt-5.6-sol/high` para uma única auditoria final após o gate consolidado. | `AGENTS.md`; Wave 005 concluída 5/5, reauditoria final sem findings |
| MSF-PL-010 | action_required | latência da extração gold | Wave 006 e tres pilotos confirmaram que gates WSL sao sub-segundo, mas leitura, auditoria e remediacao dominam. Em `E9nZMgzzxz4`, o total foi 1h09m14s: 17m44s de leitura, 19m42s de auditoria inicial, 9m de remediacoes e 21m34s de preparacao/fechamento fora dos receipts centrais. | Congelar runtime antes do timer, compactar contexto integral, revisar clusters excluidos de alto risco antes do packet, entregar um unico dossier Sol e gerar receipt final auto-verificavel. | retrospectiva `MSF-R20-GOLD-RUNTIME-PILOT-001`; plano OPT-S01..S05 |
| MSF-PL-011 | promoted | enforcement da rota one-shot | O piloto possuía payload integral e `run_gold_episode_fast.py`, mas chamou recorder e autocheck separadamente. Seis hard blockers só apareceram depois da persistência, apesar de a rota one-shot já conseguir detectá-los em memória. | Para episódio novo com payload completo, vincular `--check` e `--apply` pelo mesmo hash semântico e impedir finalização sem receipt de preview limpo. Preservar recorder direto apenas para compatibilidade e checkpoints explicitamente parciais. | validado em `E9nZMgzzxz4`: 22 reviews em uma escrita, um finalizer e hard blockers corrigidos antes do primeiro apply |
| MSF-PL-012 | mitigated | heurística numérica em português | O autocheck interpretou o artigo `uma` em `uma lead` como quantidade material e exigiu um segundo patch. Cadastrar número seria semanticamente falso. | Combinar marcadores inequívocos de quantidade e qualificadores; testar artigos `um/uma` contra contagens reais e preservar bloqueio para números efetivamente materiais. | regressões verdes para `uma lead`, `um produto`, `uma única venda`, `apenas uma venda` e `duas vendas` |
| MSF-PL-013 | promoted | job-local híbrido Windows/WSL | Os dados gold permaneceram Linux-native, mas helpers e manifests transitórios foram acessados por `/mnt/c/.../OneDrive`, com invocações variando de segundos a dezenas de segundos. | Manter job-local transitório no filesystem Linux, executar o driver one-shot em uma invocação WSL e sincronizar para Windows somente receipts e artefatos finais de consumo. | validado em `E9nZMgzzxz4`: gold, repo runtime, venv e job principal Linux-native; `/mnt` usado somente por verificadores read-only pos-epico |

| MSF-PL-014 | action_required | rederivacao pos-auditoria | No piloto `iuBPODaMz7s`, reviews corrigidos foram persistidos, mas o finalizador leu o ledger derivado do packet anterior como fonte final e bloqueou antes de reconstruir o ledger atual. | Quando o hash semantico dos reviews mudar, o autocheck do finalizador deve derivar ledger em memoria dos candidatos atuais e tratar o ledger anterior apenas como provenance substituivel pelo build. | proxima otimizacao do finalizador e regressao de review-after-packet |
| MSF-PL-015 | promoted | telemetria entre processos WSL | O receipt persistiu um contador monotonic e processos WSL posteriores produziram elapsed negativo; a VM/processo nao preservou uma base monotonic comparavel entre invocacoes. | Usar UTC wall-clock para elapsed entre invocacoes, monotonic apenas dentro do processo e registrar contagens duraveis de check/apply/finalizer/build/audit/WSL. | validado em quatro invocacoes de `E9nZMgzzxz4`: tempos nao negativos e reconciliacao completa com timestamps UTC |
| MSF-PL-016 | promoted | paridade de codigo Windows/WSL | O piloto 002 iniciou com o clone Linux sem todas as mudancas P0/P1 do checkout Windows. Os nove arquivos centrais terminaram com hashes iguais somente depois de copia seletiva manual; 908 insercoes e 52 remocoes continuam fora do commit remoto. | Sincronizar allowlist versionada sem `--delete`, incluindo o proprio manifesto, detectar drift bilateral e exigir receipt de paridade com commit, dirty hashes, venv e SHA-256 Windows/Linux iguais antes de gold write. | `sync_wsl_runtime.py`, manifesto versionado e piloto real `E9nZMgzzxz4` |
| MSF-PL-017 | promoted | quoting PowerShell para WSL | Comandos `PowerShell -> wsl.exe -> bash -lc` perderam aspas e expandiram `$VAR`, `$(...)`, pipes e filtros `jq`, causando varias invocacoes sem progresso. Usuario, distro e systemd estavam corretos; a falha foi o launcher multicamada. | Proibir `bash -lc`, usar `wsl.exe --cd ... --exec <binario> <args>` com argumentos separados, passar arrays pela propriedade nomeada `-CommandArguments` e mover orquestracao multipasso para CLI Python Linux com JSON file-based. | `invoke_gold_wsl.ps1`, regressao de cwd/argumentos e piloto real `E9nZMgzzxz4` |
| MSF-PL-018 | action_required | isolamento da extracao legada | O piloto abriu `insights_v2.json` como indice adversarial durante a composicao gold. Os 91 arquivos legados somam apenas 4,9 MiB; o custo e vies/contexto, nao armazenamento. Eles ainda sustentam masters, curated retrieval e rollback. | Proibir leitura do conteudo legado na extracao e auditoria gold; manter somente fingerprint read-only. Fazer comparacao antiga/gold em job separado pos-packet e arquivar/deletar o legado apenas depois da migracao integral dos consumidores e gate irreversivel do owner. | epic `MSF-R20-GOLD-RUNTIME-CONVERGENCE-001` e futuro epic de migracao gold |
| MSF-PL-019 | action_required | representacao do contexto semantico | Em `E9nZMgzzxz4`, 166.792 bytes de texto viraram 312.477 bytes model-facing por repeticao de chaves JSON e IDs completos. Uma representacao por indice curto preservou o texto em 177.141 bytes, reducao medida de 43,31%. | Emitir contexto integral compacto por clean-index, hidratar IDs/quotes no compilador e provar reconstrucao byte/ordem do transcript. | retrospectiva `MSF-R20-GOLD-RUNTIME-PILOT-001`, OPT-S02 |
| MSF-PL-020 | action_required | superficie da auditoria final | O packet final tinha 1.149.692 bytes e o bundle 199.027 bytes; a auditoria inicial consumiu 19m42s navegando transcript, candidatos e derivados distribuidos. | Gerar um unico dossier cego de no maximo 250 KB com transcript integral compacto, todos os candidatos, mapa de evidencias, calibracao, riscos, hashes e gates assinados. | retrospectiva `MSF-R20-GOLD-RUNTIME-PILOT-001`, OPT-S04 |
| MSF-PL-021 | action_required | recall de exclusoes low_signal | O ledger final ainda continha 341 segmentos sinalizados excluidos em 120 clusters. As tres omissoes reais estavam nesse universo; um score deterministico reduziu a revisao prioritaria a 18 clusters no limiar 8. | Criar risk-recall view pre-packet, adicionar sinais de proeminencia lexical e fixar as tres omissoes como regressoes, sem transformar incerteza em candidato automatico. | retrospectiva `MSF-R20-GOLD-RUNTIME-PILOT-001`, OPT-S03 |
| MSF-PL-022 | action_required | fechamento pos-complete | Depois de `complete/passed`, 11m02s foram gastos em verificador ad hoc, ajuste de schema de fingerprints, retries de shell e documentacao manual. | Fazer `CompleteAudit` produzir e validar um receipt canonico com status, hashes, packet, fingerprints, metricas e resumo Markdown; nao criar verificadores por episodio. | retrospectiva `MSF-R20-GOLD-RUNTIME-PILOT-001`, OPT-S05 |
| MSF-PL-023 | validated | nucleo gold otimizado | O piloto `academyhls-4faeb011-a33d-434a-8463-b366bf0c06b8` processou 676 segmentos e 8 chunks em 8m28s no nucleo instrumentado, com 27 candidatos, uma persistencia, dossier de 111 KB e auditoria inicial passed/0. O epico completo observado no Codex levou 32m08s. | Manter OPT-S01..S05 para o nucleo, mas nao declarar meta end-to-end atingida ate instrumentar selecao, preflight e fechamento. | retrospectiva e analise `MSF-R20-GOLD-RUNTIME-PILOT-002` |
| MSF-PL-024 | promoted | fronteira da telemetria do epico | O timer iniciou no bootstrap e cobriu apenas 8m28s dos 32m08s observados; 23m40s ficaram sem atribuicao exata. | O launcher agora captura UTC antes do sync; selection, preflight, prelint, preview, apply, audit e closeout ficam na sessao e no completion receipt ate a resposta gerada. | P0 validado por regressao e receipt schema 1.2 |
| MSF-PL-025 | promoted | selecao e descoberta WSL pre-bootstrap | Houve pelo menos 7m46s entre o primeiro artefato seletor e o bootstrap; o token restrito nao enxergou a distro do usuario e a selecao/request foram job-local ad hoc. | `SelectBootstrap` usa contrato explicito `Ubuntu-24.04/luish`, sem enumeracao de distro, e `--select-next` gera selecao, hashes, IDs, request, contexto e manifesto em uma chamada. | dry-run capturavel e paridade Linux 280 arquivos, zero conflitos |
| MSF-PL-026 | promoted | pre-lint do payload gold | O primeiro check encontrou sete candidatos de framework sem steps; o segundo encontrou dois records numericos com evidencia ampla e um cluster de risco sem decisao. As correcoes foram seguras, mas consumiram 1m59s de wall time. | `--prelint` executa compilador e autocheck em memoria, sem preview receipt, e reune steps, numeros, risco e warnings de evidencia ampla antes do check oficial. | regressao de steps e suporte numerico; suite gold 113 passed |
| MSF-PL-027 | promoted | fechamento ainda manual | A sessao core terminou as 18:26:47, mas receipt Windows e retrospectiva surgiram somente entre 18:28:17 e 18:28:49; parte adicional da resposta nao foi medida. | CompleteAudit agora gera receipt, resumo, performance report e resposta final e pode espelhar atomicamente somente os artefatos finais verificados para Windows. | regressao de completion e mirror; suite gold 113 passed |
| MSF-PL-028 | promoted | verify da paridade WSL | O primeiro `Action Verify` apos o P0 falhou porque o CLI tratava o caminho textual do receipt retornado pelo modo `--check` como objeto JSON. | No resumo CLI, carregar o receipt persistido quando o resultado read-only trouxer apenas seu caminho; manter `--check` sem escrita. | regressao dedicada e Verify real `read_only=true`, 280 arquivos, zero copias e zero conflitos |
| MSF-PL-029 | promoted | selecao automatica do piloto gold | O seletor 800-1.200 retornou zero elegiveis, embora o episodio mais proximo tivesse 1.233 segmentos, apenas 33 acima do limite; foi necessario um retry manual 500-1.600 antes do bootstrap. | Manter uma fila persistente por tema e duracao, recalculada apenas quando o inventario mudar; o SelectBootstrap consulta o primeiro item ainda sem gold e valida somente seus tres arquivos fonte. | `gold_episode_priority.py`, fila de 175 episodios e selecao real em 106ms |
| MSF-PL-030 | promoted | estabilidade do inventario prelint | Evidencia estreitada alterou a previa do ledger e revelou seis clusters de risco somente no prelint seguinte. Tres preflights foram necessarios e consumiram 4m38s de ciclo sem que o processamento deterministico passasse de 113ms. | Calcular o fixed point read-only de evidencia, ledger automatico e risk-recall antes de devolver o inventario; apresentar todos os acknowledgements exigidos em uma unica resposta estruturada. | fixed-point prelint e regressao support-only; suite gold 120 passed |
| MSF-PL-031 | promoted | triagem de warnings antes do apply | O primeiro prelint do piloto 003 ja mostrava alinhamento lexical fraco em G008; a auditoria final corrigiu exatamente esse ponto, removendo apenas `lideranca`. O warning era valido e permaneceu visivel, mas nao foi consumido antes da escrita. | Exigir uma disposicao source-backed para cada claim-evidence warning antes do apply, sem converter ambiguidade editorial em hard blocker; mostrar claim e minimal quote lado a lado. | warning_id/disposition no prelint, finalizer e packet; suite gold 120 passed |
| MSF-PL-032 | promoted | autoridade terminal do completion receipt | O receipt self-validating marcou `complete/passed` as 23:59:36, mas documentacao e a sequencia Verify/Sync/Verify continuaram ate aproximadamente 00:05:29, adicionando 5m53s; apenas 18,64s eram runtime de comandos. | Quando o receipt validar lifecycle, audit, packet, fingerprints e hashes, espelhar os artefatos finais uma vez e encerrar; so executar verificacao adicional se o receipt falhar ou a fonte mudar. | terminal_authority no CompleteAudit e regressao de receipt; suite gold 120 passed |
| MSF-PL-033 | promoted | meta de tempo proporcional ao volume | O piloto 003 exigiu 11m01s apenas para ler 1.233 segmentos e compor 44 candidatos. A fila pendente tem 143 episodios com ate 700 segmentos e 32 acima de 1.300, portanto um SLA universal de 10 minutos mistura clips curtos e entrevistas de horas. | Registrar faixa de tamanho no receipt e usar metas 6-10m ate 700 segmentos, 11-15m entre 701-1.300 e 18-30m acima de 1.300; manter 10m como media do inventario, nao garantia por episodio longo. | performance_budget no receipt/relatorio e regressao por porte; suite gold 120 passed |
| MSF-PL-034 | promoted | completude do dossier de auditoria | O primeiro dossier do piloto 004 declarava 1.362 entradas de ledger, mas entregava somente contagens agregadas. A auditoria Sol bloqueou corretamente a verificacao semantica do ledger. | Incluir grupos de ledger que reconstruam 100% dos clean indexes, dispositions, candidate_ids e motivos; validar a reconstrucao contra o gold antes da auditoria. | `gold_final_audit_bundle.py`; dossier final com 1.396 entradas e auditoria passed/0; suite Fast Path 102 passed |
| MSF-PL-035 | promoted | binding explicito do launcher WSL | Argumentos `--video-id` passados soltos ao `.ps1` foram absorvidos como parametros posicionais do PowerShell, corrompendo Distribution/LinuxUser/LinuxRepo antes do runtime. | Exigir array explicito em `-CommandArguments`, usar `wsl -d/-u` e falhar cedo quando parametros Linux comecarem por hifen. | `invoke_gold_wsl.ps1` e regressao `direct_wsl_launcher`; chamada real do piloto 004 passou |
| MSF-PL-036 | promoted | remediacao de reviews completos | O payload `gold_episode_compact_v2` omite chunks que ja possuem review. Depois da auditoria, o prelint retornou zero drafts e o one-shot colidiu com um receipt historico de payload vazio, embora os reviews remediados estivessem validos. | Usar `run_gold_episode_fast.py --remediate --patch`: validar o estado composto, aplicar a revisao transacionalmente, refinalizar e emitir o dossier sem reentrar no recorder inicial. | rota canonica e regressao idempotente do piloto 004 |
| MSF-PL-037 | promoted | PATH misto dentro do WSL | Uma verificacao com `rg` resolveu para WindowsApps; depois o capturador escolheu `/mnt/c/Program Files/nodejs/npx.cmd` e cinco videos falharam com `Exec format error`. | Resolver executaveis por plataforma e lancar o pipeline por `/usr/bin/env` com HOME, locale, PATH Linux e `PYTHONNOUSERSITE` explicitos; WSL nunca seleciona `.cmd`. | `invoke_gold_wsl.ps1`, capturador e regressoes de argv/plataforma |
| MSF-PL-038 | promoted | preflight e circuit breaker da rota de navegador | O probe unico classificou `system_library_missing`, abriu o circuit breaker e o teste com cinco itens provou uma chamada global e zero tentativas UI por video. | Manter a rota headless indisponivel ate um probe completo abrir Chromium e ler DOM; usar Chrome real enquanto isso. | `backfill_vturb_transcripts.py`; 24 testes verdes; probe real de 15,2 s |
| MSF-PL-039 | promoted | telemetria do backfill de transcricoes | O ledger agora separa inventory, navegacao, descricao, painel, serializacao, download, load, inferencia, validacao, promocao e gold com `run_id` e `attempt_id`. | Preservar UTC entre processos, monotonic dentro do processo e receipts por fase para ETA e retrospectiva. | 165 eventos Chrome e ledger WSL por fase |
| MSF-PL-040 | promoted | contaminacao por painel de transcricao anterior | Vinte dos cinquenta arquivos nao vazios tinham timestamps que voltavam no fim, evidenciando segmentos anexados de outro video. Sem o gate, seriam tratados como transcricoes validas. | Sempre abrir o botao da descricao do video atual, conferir video ID, ordem monotonic, cobertura, tamanho e hash antes da promocao; nunca sobrescrever o arquivo anterior antes do gate. | `backfill_vturb_transcripts.py`, capturador endurecido e regressoes de stale tail |
| MSF-PL-041 | promoted | checkpoint do Chrome por video | A varredura gravou JSON e JSONL apos cada item; uma chamada de vinte excedeu cinco minutos, mas 37 resultados ficaram preservados e a retomada pulou todos os terminais. | Manter manifesto hash-bound, resultado atomico por video e waves de no maximo oito itens para caber no limite da conexao. | 128 resultados, cursor zero, 75,1 min de wall-clock |
| MSF-PL-042 | promoted | economia e capacidade do ASR local | O benchmark quente separou load e inferencia: `large-v3-turbo` RTF 0,6335; `small` e batch reprovaram o gate literal. O primeiro episodio integral usou tres receipts e RTF quente de 0,416-0,492. | Usar somente large-v3-turbo CPU/int8 padrao, chunks de 1.200 s, overlap 2 s, reuso de modelo, prefetch de um audio e ETA por chunk. | benchmarks em `exports`; `p78Zv3_WCsM` com 998 segmentos e 99,78% de cobertura |
| MSF-PL-048 | promoted | falso no-ui por superficie visivel | A leitura inicial via DOM visivel classificou `AqzF_M2mM04` e `NiT0-ABoVnk` como sem painel porque o botao estava abaixo da dobra. O snapshot completo e locator visivel recuperaram 395 e 1.087 segmentos. | Expandir a descricao, usar snapshot completo, locator unico visivel, uma retomada apos timeout e somente entao aceitar `no_ui`. | regressao Chrome v3; ambos capturados |
| MSF-PL-049 | accepted_limitation | latencia do Chrome real | A meta de p50 <= 5 s nao foi atingida: p50 global 23,86 s e unico no-ui 11,5 s. Navegacao, expansao e painel somaram quase todo o custo; serializacao teve p50 15,8 ms. | Aceitar a latencia para preservar recall; otimizar apenas se surgir API/controle que mantenha o mesmo gate de descricao e painel real. | 165 eventos, 1,193 h somadas, zero perda por interrupcao |
| MSF-PL-050 | accepted_limitation | ETA do residual ASR | Depois do Chrome e de um episodio ASR integral, restam 94,42 h de midia. O RTF quente observado projeta cerca de 42 h no hardware CPU atual; nao ha GPU NVIDIA no WSL. | Tratar o residual como execucao longa retomavel, nunca reduzir qualidade para cumprir wall-clock; reconstruir gold uma unica vez no gate final. | 127/163 validos; 36 pending_asr |

| MSF-PL-043 | promoted | comparacao de duracao por chunks | Pilotos 003 e 004 pareciam similares por 21 contra 25 chunks, mas tinham 1.233 contra 2.349 segmentos, diferenca de 90,5%. Comprimir a leitura longa manteve o tempo inicial em 11m, mas deixou oito findings para tres auditorias. | Dimensionar SLA por segmentos, manter 18-30m acima de 1.300 e fazer uma passagem consolidada de fechamento semantico antes do primeiro packet. | plano de otimizacao do piloto 004, prompt, skill e contrato |
| MSF-PL-044 | promoted | partida certificada WSL | No piloto 005, `Verify` detectou paridade stale e foi seguido por `Sync`, uname, Python, verificador e SelectBootstrap em chamadas separadas. Os comandos consumiram segundos, mas a janela antes da selecao foi 7m00s. | Usar `StartEpisode`: sync condicional e `exec-after` substituem o mesmo processo pelo runner certificado, com data root/job-dir Linux-native e um unico run_id. | OPT-005-P0-01; parser, dry-run, preflight WSL real e regressao passaram |
| MSF-PL-045 | promoted | identidade de risk recall residual | Depois de ampliar evidencia, `risk-294617...` virou `risk-00be58...`; depois de capturar 266-269, `risk-96639...` virou `risk-037c1e...`. A decisao sobre o residual incidental continuava igual, mas o novo ID exigia outra rodada. | Preservar source lineage e escopo de segmentos por acknowledgement; escolher a disposicao residual mais especifica apenas quando o novo residual for subconjunto e nunca herdar para material novo. | OPT-005-P0-02; prelint real passou de 2 blockers para zero sem mudar candidatos |
| MSF-PL-046 | promoted | superficie de retorno do prelint | O terceiro prelint precisava comunicar dois IDs low-signal e calibracao 9/12, mas gravou 155 KB e imprimiu o autocheck integral, repetindo transcript e inventarios. | Com `--output`, manter full report no arquivo e limitar stdout a resumo esparso menor que 8 KB, inclusive resumindo calibracao. | OPT-005-P0-03; prova real: stdout 3.737 bytes e full report 145.146 bytes |
| MSF-PL-047 | promoted | custo fixo do payload autoral | O episodio tinha apenas 395 segmentos e 17 candidatos, mas o payload expandido chegou a 31,6 KB e a leitura/composicao ate o primeiro prelint levou cerca de 8m07s. A reducao foi pequena frente ao piloto de 2.349 segmentos. | Usar compact v3 com defaults por episodio/tipo, aliases locais, ranges e number shorthand; expandir deterministicamente para v2 e nunca redigitar quotes. | OPT-005-P1-01; equivalencia v3/v2 coberta por regressao |
| MSF-PL-051 | promoted | telemetria como autoridade de fechamento | Reconstrucoes manuais misturavam runtime de comandos com tempo de julgamento e deixavam lacunas de bytes por artefato. | Gerar `runtime_retrospective.md` no completion com wall time, command time, judgment gap, bytes e contagens sob o mesmo run_id. | OPT-005-P1-03; regressao de performance receipt |
| MSF-PL-052 | promoted | completude numerica parcial | Em `AqzF_M2mM04`, G004 e G016 ja tinham `numbers`, mas omitiram 86,85%, 1.2x e a cadeia 40%/35%/40% ainda presente na evidencia. O autocheck detectava ausencia total, nao cobertura de cada mencao material. | Gerar matriz candidato-mencao-record e bloquear lacuna material antes do apply, preservando disposicao para numero incidental e caveat de ASR. | OPT-006-P0-01 e fixtures dos findings AQZF-001/002 |
| MSF-PL-053 | promoted | continuidade apos auditoria final | A primeira auditoria retornou `changes_requested/2` e o fluxo encerrou o turno, exigindo nova ordem do owner para remediation. | Tratar audit, remediation, reauditoria e completion como estados internos do mesmo epico; somente receipt `complete/passed/0` ou bloqueio externo permite resposta terminal. | OPT-006-P0-03 |
| MSF-PL-054 | promoted | escopo da paridade WSL | Uma mudanca de status no plano invalidou a paridade antes da remediation, embora nenhum executavel tivesse mudado. | Separar `execution_signature` de `documentation_signature`; somente drift executavel bloqueia gold write. | OPT-006-P0-04 |
| MSF-PL-055 | promoted | asserts de remediation | O primeiro check do patch AQZF comparou campos `legacy_*` derivados com a fonte manual e falhou sem escrita. | Gerar asserts diretamente dos reviews fonte, removendo derivados e copiando strings atuais byte a byte. | OPT-006-P0-02 |
| MSF-PL-056 | promoted | atribuicao de tempo ativo | O receipt atribuiu uma pausa de 7h15m a prelint e outra de 24m51s a remediation; o total observado foi 8h31m, mas a atividade verificavel somou cerca de 51m. | Registrar spans de inicio/fim e separar `inter_turn_idle_ms`, runtime, julgamento e transicao. | OPT-006-P1-01 |
| MSF-PL-057 | promoted | congelamento do benchmark | O piloto 005 comecou antes de P0/P1 estarem integralmente no clone Linux e foi retomado depois de mudancas em scripts centrais. O resultado e valido, mas o tempo mistura duas versoes. | Congelar a assinatura executavel antes da selecao e invalidar somente o benchmark, nunca o gold valido, se codigo mudar durante a janela. | OPT-006-P0-04 e criterio do piloto 006 |
| MSF-PL-058 | promoted | cwd do exec-after WSL | O primeiro StartEpisode certificado do piloto 006 executou o Python Linux, mas herdou o cwd `/mnt/c` e importou scripts do checkout Windows. | Antes de `execv`, o sincronizador deve mudar explicitamente para o clone Linux certificado; a regressao captura e compara o cwd do processo substituido. | `sync_wsl_runtime.py`; 2 regressoes focadas passaram no WSL |
| MSF-PL-059 | promoted | custo semantico domina o piloto | `p78Zv3_WCsM` terminou em 50m18s, enquanto todos os comandos deterministas registrados somaram 2,05s; auditoria e composicao encontraram tres correcoes reais e consumiram praticamente toda a janela. | Otimizar representacao e navegacao semantica, nao relaxar leitura: gerar mapa de fronteiras/gaps e payload incremental source-canonical antes da auditoria, mantendo dossier integral como prova. | performance report do piloto 006; completion `complete/passed/0` com 24 IDs |
| MSF-PL-060 | promoted | recall nas caudas adjacentes | Os dois findings de recall do piloto 006 estavam imediatamente depois da evidencia capturada: G014 terminava em 762 e omitia 763-770; G020 terminava em 912 e omitia 913-923. Varios indexes nao existiam no signal ledger. | Antes do packet, revisar e dispor janelas adjacentes a cada evidencia, fronteiras de chunk e a cauda final do episodio, sem depender apenas do signal inventory. | plano piloto 007; fixtures equivalentes aos findings P78Z-001/002 |
| MSF-PL-061 | promoted | relacao por contencao de evidencia | G010 tinha 683-703 como suporte e G011 tinha exatamente 683-703 como evidencia minima, mas nenhum parent/child foi registrado. A heuristica lexical de overlap nao representava essa estrutura. | Gerar grupos de contencao de evidencia e exigir decisao semantica explicita; nunca criar relacao automaticamente. | plano piloto 007; fixture P78Z-003 |
| MSF-PL-062 | promoted | custo da autoria de remediation | O runtime da remediacao foi 442ms, mas a janela levou 21m45s por helper/manifesto manual, drift de runtime e evidencia que incluiu um `100%` incidental. | Gerar scaffold audit-to-patch com quotes e asserts source-canonical, simular numeric scope/ledger/relacoes antes do apply e congelar o runtime por run_id. | plano piloto 007; dossier remediado passou na primeira reauditoria |
| MSF-PL-063 | promoted | spans semanticos ausentes | A telemetria atribuiu 48m43s a idle e somente 23ms a model judgment porque leitura, autoria e auditoria nao tinham eventos de inicio/fim. | Registrar spans explicitos de leitura/autoria, auditoria, remediacao, reauditoria e closeout; reconciliar wall sem inventar tempo interno do modelo. | OPT-007-P1-01 implementado; regressao de reconciliacao passou no WSL |
| MSF-PL-064 | promoted | duplicacao no DOM de transcricao do YouTube | A captura pelo Chrome real retornou cada segmento duas vezes em `9wfKG0bf53o` e `HRuMLbkMO0E`: uma ocorrencia com duracao zero e outra com a duracao efetiva, ambas com o mesmo inicio e texto. | Preservar a captura bruta, deduplicar o original canonico pela chave exata `(start_seconds, text)`, manter a maior duracao e proteger ambos os arquivos por fingerprint. | `dedupe_transcript_original.py`; 1.248->624 e 910->455; cobertura preservada |
| MSF-PL-065 | promoted | traducao gold separada da fonte | Episodios em ingles precisam continuar auditaveis na lingua publicada, mas a extracao gold deve operar em pt-BR; a traducao do YouTube nao atende ao contrato decidido pelo owner. | Manter `transcript_original.json` imutavel como fonte, produzir `transcript_pt_br.json` com `provider=codex:faithful_translation`, vincular pelo SHA-256 e fazer o gold preferir a traducao validada. | tres episodios en/pt-BR validados; hashes iguais; 178 testes verdes |
| MSF-PL-066 | promoted | volume de fechamento semantico | O piloto 007 produziu 80 superficies de semantic closure; a classificacao manual levou o reparo prelint a 25m13s, embora os comandos somassem menos de um segundo. | Priorizar resultado numerico, mecanismo, before/after e outcome; agrupar caudas genericas ja cobertas e manter prova source-backed sem exigir decisao repetitiva. | `beFYVzSv2bw`; 20 chunks, 35 candidatos iniciais e prelint final limpo |
| MSF-PL-067 | promoted | multiplicidade numerica na remediacao | O scaffold nao informou que G037 precisava de um record por ocorrencia nem preservou o token completo `40 semana`; dois checks read-only falharam antes do apply. | Incluir no repair scaffold a matriz de ocorrencias, canonical, kind, raw literal esperado e quantidade de records por candidato sequencial. | remediacao 003 passou com 14/14 records cobertos e zero hard blocker |
| MSF-PL-068 | action_required | invariantes da reauditoria delta | Inserts G036/G037 foram classificados como candidatos nao afetados e metadados derivados fizeram fingerprints/calibracao divergirem no delta, apesar de o dossier integral e o receipt final provarem invariantes validos. | Derivar affected IDs pelo diff/ranges, comparar o conteudo protegido e excluir campos derivados de packet/build; manter fallback para dossier integral ate a correcao. | delta `7869c7...` rejeitado; dossier v3 e completion `complete/passed/0` aprovados |
| MSF-PL-069 | action_required | custo da auditoria longa | O dossier v3 reduziu navegacao duplicada, mas a auditoria integral de 1.989 segmentos ainda levou 19m39s, acima do alvo de 6-8m. | Criar um mapa Sol de risco ordenado e colapsar warnings ja dispostos sem remover transcript integral, candidatos, numeros, ledger ou calibracao. | piloto 007, dossier 455.764 bytes, quatro findings reais corrigidos |
| MSF-PL-070 | promoted | fechamento semantico orientado por risco | Em `eCaODMtU5GY`, 230 linhas viraram 224 warning IDs e consumiram 29m27s; a disposicao generica marcou como incidental a trajetoria material de 2023 que depois originou G049. | Separar `must_close` de `audit_only`, deduplicar por lineage e exigir source scope para incidental de alto risco. | autocheck, runner, skill e regressao piloto 008 |
| MSF-PL-071 | promoted | matriz numerica antes do packet | Seis ciclos sucessivos reduziram blockers numericos de 112 para 22, 3 e 2; ainda chegaram a auditoria um comparador omitido, duracao 10-12 meses e `raw=30` com valor de dois minutos. | Compilar matriz candidato-ocorrencia sobre evidencia e adjacencias, incluindo sequencias, comparadores, anos e coerencia raw/value/unit, antes do prelint oficial. | autocheck, repair manifest e regressao raw/value |
| MSF-PL-072 | promoted | suporte de claim e counterexample | G027 afirmava autoedicao/reducao de elenco/gravacao em casa sem 612-616; G008 omitia a demonstracao falha imediatamente relacionada. | Comparar elementos do claim com minimal/support evidence e revisar uma a tres unidades adjacentes para negacao, caso falho, excecao e caveat antes do packet. | semantic closure e fixtures piloto 008 |
| MSF-PL-073 | promoted | transporte do audit Windows para Linux | O audit Sol foi gravado primeiro no mirror Windows; UNC, `/mnt/c` e `C:\\tmp` falharam ou travaram, adicionando 3m06s antes da remediacao. O runtime WSL em si permaneceu rapido e correto. | Materializar envelope UTF-8 validado diretamente no job Linux por base64 argv e espelhar somente depois do receipt terminal. | recorder de audit e regressao zero-write |
| MSF-PL-074 | promoted | invariantes da reauditoria delta | No piloto 008, assim como no 007, um insert valido de finding sem candidate ID foi classificado como nao afetado; `verified_at` alterou o agregado de fingerprints e ledger derivado foi confundido com transcript fonte. | Derivar affected IDs por audit, diff e ranges; comparar apenas conteudo protegido, excluir metadata derivada e limitar transcript imutavel aos campos fonte. | delta canonico e fixture de insert por range |
| MSF-PL-075 | promoted | telemetria sem spans semanticos | O receipt terminal mediu 1h07m51s e apenas 6,588s de comandos, mas `semantic_spans=[]` fez 65m50s aparecerem como `inter_turn_idle_ms`. | Registrar spans de leitura, reparo, auditoria, remediacao, reauditoria e closeout; quando ausentes, usar `unattributed_gap_ms`, nunca idle. | runner, completion e regressao de reconciliacao |
| MSF-PL-076 | validated | arquitetura WSL do gold | Selecao, preflight e contexto de `eCaODMtU5GY` levaram 9,127s e os comandos deterministas totais 6,588s, sem erro de startup, quoting ou fallback Windows. | Preservar WSL Linux-native; otimizar apenas a fronteira de artefatos transitorios e nao reverter a arquitetura por causa da latencia semantica. | piloto 008 complete/passed/0 |
| MSF-PL-077 | validated | fechamento semantico orientado por risco | No piloto 009, tres prelints e 6m18,7s substituiram dez ciclos e 29m27s do piloto 008, reducao de 78,6%. | Preservar risk tiers, matriz numerica, fixed point e dry-run; simplificar a superficie entregue ao modelo em vez de reverter o nucleo. | piloto 009 e plano OPT-010 |
| MSF-PL-078 | implemented | complexidade model-facing | O piloto 009 gerou contexto de 292 KB, prelint de 2,71 MB, dossier de 893 KB, 187 superficies de risco e sete helpers job-local; o total subiu 9,7% apesar do ganho de prelint. | Consolidar cobertura, binding e repair manifest em uma unica visao compacta; full reports ficam apenas para prova e debug. | `semantic_workbench` compartilhado por prelint/brief/dossier; 163 testes verdes; benchmark real pendente |
| MSF-PL-079 | implemented | recall de blocos de produto | Cinco findings materiais chegaram a auditoria em blocos tardios de economia KYC, trial product-led, feedback de clientes, valor percebido e PMF. | Priorizar blocos economicos e de produto sem candidato e gerar binding claim/evidence/calibration antes do primeiro packet. | fixtures de economia, produto, evidence drift e PMF no workbench; benchmark real pendente |
| MSF-PL-080 | implemented | custo do risk brief na auditoria | Mesmo com brief de 50 KB, a auditoria inicial levou 16m48s porque ainda exigiu navegar o dossier de 893 KB e tabelas separadas. | Entregar workbench de auditoria em duas camadas com cobertura, claim/evidence/numbers/caveats/calibration e janelas source-complete por indice. | dossier 3.1 e risk brief preferem o workbench; full transcript continua obrigatorio |
| MSF-PL-081 | implemented | scaffold de remediacao source-canonical | Dois checks read-only e helpers especificos foram necessarios porque raw ASR e escopo numerico aproximados nao coincidiam com a fonte atual. | Compilar findings em scaffold oficial com asserts, raw, ranges, matriz numerica e redirects copiados do estado canonico; nenhuma decisao automatica. | scaffold 1.1 com numeros, evidence ranges, review/calibration asserts e patch template; regressao verde |
| MSF-PL-082 | implemented | telemetria da leitura e composicao | O receipt reconciliou 74m26,5s, mas 40m32,6s ficaram nao classificados e o intervalo contexto-prelint de 36m16,8s nao separou leitura, composicao, helpers e pausas. | Registrar spans automaticos de leitura e composicao; gap acima de 10% invalida apenas o benchmark, nunca o gold. | `StartEpisode` abre automaticamente `semantic_reading_and_authoring`; regressao verde; medicao real pendente |
| MSF-PL-083 | promoted | arquitetura canonica apos compilador cego | O melhor compilador cego ficou em 88,24% de recall material, uma atribuicao sem suporte e tres findings; o inventario melhorou numeros/relacoes, mas shards e auditoria consumiram 6m51s. | Manter leitura cronologica e autoria principal; promover apenas inventario numerico, bindings, disposicoes escopadas e bloqueio de duplicata exata. Arquivar shards/reducer/gap resolver como pesquisa read-only. | `chronological_hybrid_v1`, assinatura de runtime separada, regressao de duplicata e documentacao canonica |
| MSF-PL-084 | promoted | benchmark real do pacote OPT-010 | No episodio `NiT0-ABoVnk`, prelint caiu 66,8% e a primeira auditoria caiu 42,1%, mas o wall total subiu de 74m26,5s para 9h43m33,4s e findings de classes ja conhecidas chegaram ao primeiro packet. | O pacote fragmentado foi substituido por `gold_authoring_manifest_v1`, view adversarial unica e regressoes pre-write de F019/F037. O ganho de wall permanece pendente de dois pilotos reais congelados. | `gold_authoring_manifest.py`, `run_gold_episode_fast.py`, contrato/prompt/skill gold e `test_gold_runtime_simplification_011.py` |
| MSF-PL-085 | promoted | fragmentacao da remediacao gold | O job acumulou 182 arquivos, 27 helpers Python, nove finalizacoes, seis builds e cinco audit bundles; o episodio cresceu de 75 para 142 candidatos depois do primeiro apply. | O caminho normal proibe autoridade paralela de ledger/calibracao e substitui o snapshot completo do manifesto em uma unica transacao; derivados sao recompilados do mesmo hash. | manifesto autoral, remediacao one-shot e regressao de proibicao de campos paralelos |
| MSF-PL-086 | promoted | continuidade e medicao da auditoria final | O span de reauditoria registrou 8h06m59,2s porque misturou espera, interrupcao e perda de continuidade do veredito; a telemetria ainda informou zero patches e zero remediacoes apesar das correcoes realizadas. | Request/envelope hash-bound foram implementados; retomada reinicia apenas Sol, spans interrompidos sao medidos separadamente e reauditoria usa delta com fallback automatico ao dossier integral. | `gold_audit_lifecycle.py`, completion envelope-first, telemetria 1.4 e regressao durable-resume |
| MSF-PL-087 | promoted | identidade terminal do episodio | A documentacao do piloto 009 registra `NiT0-ABoVnk` como completo, enquanto a fila canonica voltou a seleciona-lo e abriu outro run valido. A coexistencia indica que fila, data root, receipt terminal e fingerprint de fonte nao foram reconciliados por uma identidade unica. | O seletor consulta identidade terminal compativel com a fonte ativa; fila e cursor sao apenas ordenacao e fonte alterada exige `--explicit-reprocess` com motivo. | `gold_terminal_identity.py`, seletor fast e regressoes stale/source-changed |
| MSF-PL-088 | action_required | estabilidade de warnings e gates na remediacao | No benchmark 011, uma edicao local source-backed alterou warning IDs e reabriu gates de calibracao nao afetados. Checks sucessivos consumiram julgamento e tornaram a remediacao maior que o finding original. | Tornar warning IDs dependentes da proposicao e fonte local, nao do snapshot global; a substituicao transacional deve revalidar somente candidatos/calibracoes afetados e preservar decisoes validas por hash de entrada local. | benchmark real da Simplificacao 011; dois episodios completos, mas wall 3h34m/3h08m |
| MSF-PL-089 | promoted | repeticao oral numerica versus nova observacao | Em G044 de `fBaX4ixKkFo`, o reconciliador exigiu um segundo record de 20 slots e confundiu a repeticao oral da capacidade com os records posteriores de 20 vendas. | Permitir reuso de um record apenas quando a caveat humana declara `source duplicate` e `not a second observation`, o token e bare, o valor/unidade coincidem e a ocorrencia permanece visivel como `covered_explicit_duplicate`. | regressao dedicada, suite completa 293 passed e reauditoria Sol/high passed/0 |
| MSF-PL-090 | promoted | calibracao de ocorrencia duplicada sem link lexical | Duplicatas numericas do cold open nao aparecem como `semantic_candidate_ids`, embora a decisao explicita identifique fonte duplicada e ocorrencia canonica dentro da evidencia do candidato. Exigir link lexical anulava a propria rota explicita. | Validar target duplicado, clean index canonico dentro dos ranges de evidencia, equivalencia declarada, anchor numerico e justificativa; nunca criar record ou candidato duplicado. | `calibration_decision_issues`, regressao F019 e suite completa 293 passed |
| MSF-PL-091 | action_required | confiabilidade da telemetria de remediacao | Os receipts finais registraram `remediations=0` e `patches=0` apesar de remediacoes persistidas; um span stale de prelint repair acumulou 53m04s e 1h20m-2h13m ficaram nao classificados. | Contar remediacao no commit transacional real, encerrar spans na mesma chamada atomica e invalidar apenas a medicao quando gap nao classificado exceder 10%; nao usar esses campos para alegar ganho ate a reconciliacao passar. | performance reports de `jbFY16W5GTE` e `fBaX4ixKkFo`; gold final permanece valido |
| MSF-PL-092 | action_required | auditoria iniciada antes do gate final da wave | A auditoria de `jbFY16W5GTE` iniciou enquanto `fBaX4ixKkFo` ainda estava em leitura/autoria. Os dossiers iniciais somavam 427 segmentos unreviewed e 183 must-close apesar de `hard_blockers=[]`, gerando cinco findings major e repeticao da fase Sol. | Tornar source completeness uma invariante unica de `ready` e emitir o audit request consolidado somente quando todos os ramos estiverem prontos e com hashes congelados. | benchmark 011: auditoria inicial 38m19s; remediacao + reauditoria 02 cerca de 2h03m |
| MSF-PL-093 | action_required | duplicacao model-facing no dossier | Nos dossiers finais, header mais semantic workbench ocupam 65,5% e 62,5%; `audit_navigation` e `audit_warnings` repetem ranges, quotes, claims e candidate IDs que ja existem no transcript, candidatos e workbench. | Manter um unico dossier source-complete, remover copias do header e representar warnings por referencias locais; nao criar outro brief paralelo. | dossiers finais de 909.666 e 930.916 bytes; plano HI-012-03 |
| MSF-PL-094 | validated | ganho parcial da simplificacao 011 | Contra o piloto anterior, o wall da wave caiu 63,3%, a leitura/autoria media 36,3%, os artefatos observados 62,1% mesmo com dois episodios e o crescimento pos-apply caiu de 89,3% para cerca de 13%. Ainda assim, o total ficou em 3h34m e os gaps nao classificados em 42,6%-62,3%. | Preservar identidade terminal, manifesto unico, one-shot e reauditoria focal; limitar novo trabalho as tres causas estruturais HI-012-01..03. Nao promover o benchmark como meta ponta a ponta atingida. | retrospectiva `msf-r20-gold-runtime-benchmark-011-retrospective.md` |
| MSF-PL-095 | promoted | source completeness como autoridade de ready | `hard_blockers=[]` isolado permitiu dossiers com 427 unreviewed e 183 must-close. | Usar uma invariante unica em prelint, finalizer e dossier que reconstrua o transcript, feche must-close e prove numeros/calibracao antes de ready. | HI-012-01; regressao dirigida e suite 301 passed |
| MSF-PL-096 | promoted | precondicao de remediacao antes do commit | A rota anterior descobria envelope ausente somente depois de persistir reviews, packet e dossier, deixando receipt incoerente e contadores zerados. | Validar request, envelope, artifact identity e dossier anterior antes do write; contar a remediacao no commit e declarar explicitamente qualquer estado persistido em erro posterior. | HI-012-02; envelope ausente prova zero write/build/finalizer |
| MSF-PL-097 | validated | deduplicacao por referencias no dossier | Copias de navigation, warnings, risk clusters e workbench mantinham dossiers de 910-931 KB. | Preservar uma unica fonte verbatim e representar navegacao/decisoes em tabelas de referencias, internando justificativas repetidas; despachar auditoria somente com request N/N hash-bound. | dossiers reais 457.810/491.827 B, reducao 49,67%/47,17% |
| MSF-PL-098 | promoted | completude estrutural nao prova equivalencia semantica | A invariante 012 fechou 100% do ledger e zero unreviewed, mas a Sol encontrou minimal evidence que nao expressava a claim e um reported case mal classificado. | Manter leitura cronologica e Sol final como gates obrigatorios; reabrir gold protegido somente por revisao explicitamente autorizada, preservar provenance anterior e exigir nova auditoria integral. | remediacao 012 em uma transacao por episodio e reauditoria Sol/high `passed/0` |
| MSF-PL-099 | promoted | auditoria stale e identidade terminal em revisao protegida | Uma nova finalizacao semantica reutilizou inicialmente o audit `passed` do snapshot anterior; um replay protegido tambem podia trocar identidade apenas por timestamps novos do receipt. | Toda nova finalizacao pode forcar `pending_external`; a revisao arquiva audit e identidade anteriores; a identidade so e substituida quando o status referencia explicitamente a identidade autorizada. Replay comum retorna a identidade existente sem escrita. | `gold_terminal_identity.py`, `force_pending_external`, regressao de snapshot novo e idempotencia; suite `303 passed` |
| MSF-PL-100 | validated | ganho estrutural versus qualidade de primeira passagem | A wave 007 reduziu o wall de 3h34m23s para 2h01m09s (-43,5%), mas o primeiro veredito subiu de cinco para 19 findings. Source completeness, request consolidado, dossier deduplicado e transacao envelope-first funcionaram; a autoria semantica inicial regrediu. | Preservar o nucleo 012, abandonar crescimento de candidatos como proxy de qualidade e medir convergencia por findings P0, remediacoes e dossiers ate passed/0. | analise `msf-r20-wave-007-process-analysis-013.md`; proximo benchmark exige zero finding numerico/calibracao e no maximo uma remediacao por episodio |
| MSF-PL-101 | implemented | fechamento semantico de numeros materiais | Dez dos 19 findings iniciais e seis dos oito findings apos a primeira remediacao eram numericos. Prelint aceitou raws materiais como `count/value=null`; um helper acrescentou rows tipados sem retirar os opacos. | A invariante agora exige disposicao exclusiva por ocorrencia material, rejeita fallback opaco, escala ASR inventada e representacao dupla; remediacao substitui a matriz completa. | 15 regressoes 013 aprovadas; `-46vMG3l8Jo` e `0sB3ia6LIVM` chegaram a prelint limpo com zero blocker numerico antes da unica escrita; confirmacao final depende da Sol selada |
| MSF-PL-102 | implemented | autoridade incompleta de calibration decisions | `calibration_decisions` era validado no manifesto, mas nao chegava ao payload compilado. Duplicatas materiais do cold open precisavam poluir evidence/ledger do candidato canonico. | A decisao autoral agora atravessa compilador, autocheck, build e dossier; duplicata validada deriva ledger `merged` com candidate IDs, source lineage e anchor sem ampliar evidence. | fixtures de duplicata valida/falsa e workbench merged aprovadas; E01/E02 fecharam calibracao antes do write |
| MSF-PL-103 | implemented | remediacao focal sem fecho de dependencias | Alteracoes source-complete mudavam ledger, calibracao, warning ou candidato fora do range literal e o delta era rejeitado apenas depois da derivacao. | Impact closure e calculado antes do commit; a rota escolhe delta ou dossier integral antecipadamente e substitui a revisao completa do candidato. | fixtures local/global e stale-base aprovadas; benchmark inicial gerou uma escrita, um finalizer, um build e um dossier por episodio, sem remediacao ate o gate Sol |
| MSF-PL-104 | implemented_pending_final_validation | fronteira temporal do veredito Sol | O benchmark 013 revelou que iniciar a fase Sol ainda podia deixar o span de autoria aberto, inclusive atravessando o episodio seguinte. | A materializacao fecha o span Sol no veredito; iniciar fase nova fecha a anterior no mesmo boundary e o one-shot encerra autoria quando fica ready. | 15 regressoes 013 aprovadas; dois jobs reconciliados ao timestamp factual do one-shot sem overlap; fechamento do span Sol aguarda envelope final autorizado |

## Modelo para novas entradas

~~~text
ID:
estado: observed | recurring | action_required | promoted | closed | accepted_limitation
escopo:
primeira_ocorrencia:
ultima_ocorrencia:
recorrencias_confirmadas:
sintoma:
causa_confirmada:
evidencia:
contencao_imediata:
prevencao_permanente:
promovido_para:
responsavel:
validacao:
~~~

## Revisão no fechamento de épico

No encerramento do épico, o chat ativo:

1. analisa somente observações concretas de `process_learnings`;
2. deduplica por causa confirmada;
3. atualiza contagem e estado;
4. promove para o destino mais específico, se necessário;
5. registra como a prevenção foi validada;
6. mantém a entrada aberta apenas quando ainda houver ação real.
